Define the following function that converts a non-empty list into a balanced tree

balance :: [a] -> Tree a 

Firstly define a function that splits a list into two halves whose length
differs by at least one 